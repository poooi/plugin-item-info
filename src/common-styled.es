import styled from 'styled-components'
import { SlotitemIcon } from 'views/components/etc/icon'

export const ItemIcon = styled(SlotitemIcon)`
  &&&&& {
    width: 24px;
    height: 24px;
    border: none;
  }
`
